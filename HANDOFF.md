# HANDOFF — ปรับ Kubo's Classroom ให้เป็นสนามซ้อมของคอร์ส

จาก: Napat (เจ้าของคอร์ส) · ถึง: Chosaque · 22 ก.ย. 2569

เอกสารนี้ส่งต่องานที่เหลือให้ปรับและพัฒนาต่อ ทุกข้อมีที่มา — ถ้าเป็นตัวเลข คือวัดจากของจริงแล้ว ไม่ใช่ประมาณ
ถ้าข้อไหนไม่ตรงกับที่เห็นในโค้ด ให้เชื่อโค้ดและบอกกลับ

---

## เป้าหมาย

Guesthouse (`website/guesthouse/`) จะใช้**แทนเว็บสนามซ้อมเดิมของคอร์ส** ซึ่งมีเครื่องมือ QI-01 ถึง QI-07
ลิงก์นี้จะพิมพ์ลงเอกสารผู้เรียน สไลด์ และ cheatsheet ส่วนเด็ค Agent Team (`app/`) จะลิงก์ไว้เป็นสื่ออ่านต่อหลัง Lab 5

**ผู้เรียน:** เจ้าหน้าที่งานคุณภาพและธุรการของโรงพยาบาล **ไม่มีพื้นฐานเขียนโปรแกรม**
ใช้ **Claude Code Desktop** (แอป ไม่ใช่เทอร์มินัล) · ภาษาไทยเป็นหลัก อังกฤษใช้กำกับศัพท์

**เงื่อนไขที่ห้ามพลาด 3 ข้อ**

1. **ศัพท์ต้องตรงกับที่สอนในห้อง** — ผู้เรียนจะสลับระหว่างเว็บนี้กับสไลด์ ถ้าคำเดียวกันแปลต่างกัน เขาจะสับสนตรงหัวใจของคอร์ส (ส่วนที่ 1)
2. **ห้ามอ้างว่า "AI จะทำผิดแบบนี้" ถ้าไม่ได้วัด** — คอร์สนี้รันกับ agent จริงแล้วพบว่าข้ออ้างแบบนี้ผิดเกือบทุกข้อ
   ป้ายเดิม *"เกมจำลอง · ไม่ได้เรียกใช้ AI จริง"* ดีมากแล้ว ให้คงไว้
3. **ตัวอักษรไทยตามกติกาในส่วนที่ 6** — ทุกข้อเกิดจากของที่พังมาแล้ว

---

## สถานะตอนนี้ — PR ที่เปิดไว้ (merge ก่อนเริ่มงานใหม่)

| PR | ทำอะไร | ตรวจแล้ว |
|---|---|---|
| #2 | ลบโมเดล 3D ที่ไม่มีใครเรียก 5 ไฟล์ (13.3 MB) | เสิร์ฟ `website/` ในเครื่อง — Guesthouse กับ `/live/` โหลดโมเดลครบ 0 error |
| #3 | ลบบรรทัดเฉลยออกจาก `event-kit/samples/draft-v1.md` + build zip ใหม่ | รัน checker จริง 2 รอบ ยังจับ 21 vs 20 พ.ย. ได้ทั้งคู่ |
| #4 | `npm run check:course` — ตรวจ `course-data.mjs` | ข้อมูลปัจจุบันผ่าน · ทดสอบว่าพังได้จริงแล้ว |
| #5 | ตัวอักษรไทยใน Guesthouse | วัดจากหน้าจริงทั้ง 8 บท ที่ 1280/375/320 px |

**ขอเรื่องเดียวก่อนเริ่ม:** preview ของ PR ตอนนี้ติด Vercel Authentication — ผมเปิดดูไม่ได้ (ได้ 302 ไปหน้า login ทุก URL)
ช่วยปิดที่ Project → Settings → Deployment Protection สำหรับ Preview หรือส่ง Shareable Link ให้ทีละ PR

---

## ส่วนที่ 1 — สัญญาศัพท์ (ต้องตรงคำต่อคำ)

### 4 แนวทางจัดโต๊ะ — แต่ละแนวทางผูกกับเครื่องมือเดียว

| แนวทาง | ความหมายในคอร์ส | เครื่องมือใน Claude Code | ไฟล์ | Lab |
|---|---|---|---|---|
| **Write** | จดออกนอกหัว — สิ่งที่ต้องบอกซ้ำทุกครั้ง เขียนไว้ในไฟล์ให้ agent อ่านเอง | `CLAUDE.md` | `CLAUDE.md` ที่รากโปรเจกต์ | 2 |
| **Compress** | ย่อของยาวให้สั้น — คำสั่งยาวที่พิมพ์ซ้ำ ยุบเหลือคำสั่งลัดคำเดียว | slash command | `.claude/skills/<ชื่อ>/SKILL.md` **ที่มี** `disable-model-invocation: true` | 3 |
| **Select** | หยิบเฉพาะที่ต้องใช้ตอนนั้น — agent หยิบเองเมื่อ `description` ตรงงาน | skill | **ไฟล์เดิมใบนั้น** ที่ลบบรรทัด `disable-model-invocation` ออก | 4 |
| **Isolate** | แยกงานไปอีกโต๊ะ — ส่งงานรกไปทำที่อื่น รับกลับแค่ข้อสรุป | subagent | `.claude/agents/<ชื่อ>.md` | 5 |

**จุดที่ต่างจากเว็บตอนนี้ และต้องแก้**

- **"ทางลัด / Shortcut" = Compress ไม่ใช่ Select** — ตอนนี้ QI-07 บทที่ 3 เขียนว่า *"เลือก skill ให้ตรงงาน"* ซึ่งคือ Select
  ในคอร์ส ปุ่มลัดคือ*ปุ่มที่คนกดเอง* (`/ชื่อ`) ส่วน "ตู้แฟ้ม" คือ*แฟ้มที่ agent เดินไปหยิบเอง*
- **Compress กับ Select คือไฟล์ใบเดียวกัน ต่างกันบรรทัดเดียว** — Lab 4 ทั้งแล็บคือ "ลบหนึ่งบรรทัดจากไฟล์ที่ทำใน Lab 3"
  เว็บตอนนี้ไม่มีจุดไหนสอนเรื่องนี้ (`disable-model-invocation` ไม่ปรากฏเลย) — ควรมีหนึ่งจังหวะที่ผู้เรียน*เห็น*บรรทัดนั้นหายแล้วของเปลี่ยนจากปุ่มเป็นแฟ้ม
- **Isolate = subagent** — ไม่ใช่แค่ "แยกงาน" ลอย ๆ · เหตุผลที่คอร์สใช้คือ**ประหยัดที่บนโต๊ะและเวลา**
  ห้ามใช้เหตุผล "ทำงานใหญ่แล้วมันจะลืม" — วัดแล้วไม่จริง agent ตัวเดียวทำงานปริมาณมากผ่านเกณฑ์ครบ
- ไม่ต้องใช้คำว่า Write/Compress/Select/Isolate กับความหมายทั่วไปแบบ context engineering (เช่น Compress = สรุปบันทึก)
  ถ้าจะสอนการสรุปบันทึก ให้เรียกตรง ๆ ว่า "สรุป" หรือโยงกับ `/compact` ที่ Claude Code มีให้อยู่แล้ว

### ของ 8 ชิ้นในห้องทำงาน (QI-07) — ใช้ชื่อชุดนี้ ทั้งไทยและอังกฤษ

ชื่ออังกฤษมาจากเด็คอังกฤษของคอร์ส ชื่อไทยมาจากคู่มือผู้สอน เรียงตามลำดับที่ผู้เรียนเจอ

| # | ชื่อไทย | English | ไฟล์/ของจริง | เจอตอน | แนวทาง |
|---|---|---|---|---|---|
| 1 | โต๊ะทำงาน | Desk | context window ที่กำลังใช้ | มีมาให้ | — |
| 2 | ตู้เซฟ | Memory safe | `CLAUDE.md` | Lab 2 | Write |
| 3 | ปุ่มลัดข้างโต๊ะ | Shortcut button | `SKILL.md` + `disable-model-invocation` | Lab 3 | Compress |
| 4 | ตู้แฟ้ม | Filing cabinet | ไฟล์เดิม ลบบรรทัดนั้นออก | Lab 4 | Select |
| 5 | โต๊ะอีกตัวที่เรียกมาช่วย | Extra helper | `.claude/agents/data-check.md` | Lab 5 | Isolate |
| 6 | ประตูรักษาความปลอดภัย | Door | permission และหน้าต่างขออนุมัติ | Lab 6 | human-in-the-loop |
| 7 | ช่องรับเอกสารจากข้างนอก | Document slot | MCP และไฟล์ที่ agent อ่านเข้ามา | มีมาให้ | — |
| 8 | กระดานงาน | Task board | รายการงานที่ Claude Code แสดงระหว่างทำงานยาว | มีมาให้ | — |

**ใน Claude Code Desktop** — อย่าอ้างกลไกเทอร์มินัล: ไม่มี `shift+tab` (ใช้ตัวเลือกโหมด), ไม่มี `/agents` (ถูกถอดแล้ว)
โหมด `default` ในแอปเรียกว่า **Manual** · `/context` คือ **วงแหวนการใช้งาน** · `/clear` คือ **New session**

---

## ส่วนที่ 2 — เนื้อเรื่องโรงพยาบาล

ตกลงกันแล้ว: **คงตัวละครและโมเดล 3D เดิม** เปลี่ยนแค่เรื่อง

- **ฉาก:** ศูนย์คุณภาพ โรงพยาบาลศรีสมบูรณ์ (โรงพยาบาลสมมติของคอร์ส)
- **Kubo** = agent (Claude Code) · **Luma / Pip / Nova** = คนในโรงพยาบาลที่มาขอให้ช่วยงาน
  เช่น หัวหน้าศูนย์คุณภาพ · หัวหน้าหอผู้ป่วย · เจ้าหน้าที่เวชระเบียน — เลือกเองได้ ขอแค่งานที่ขอเป็นงานคุณภาพจริง
- **งานที่เหมาะเป็นโจทย์:** สรุปตัวชี้วัดรายเดือน · อ่านรายงานอุบัติการณ์ · สรุปมติประชุมทีมนำคุณภาพ · ร่างหนังสือเวียน

**ข้อเท็จจริงที่ต้องถูก ถ้าหยิบมาใช้**

- อัตราการติดเชื้อในโรงพยาบาล = จำนวนครั้งที่ติดเชื้อ ÷ **วันนอนรวม** × 1,000 (หน่วย: ครั้งต่อ 1,000 วันนอน)
  **ไม่ใช่** ÷ จำนวนผู้ป่วย × 100 — ไฟล์ `app/kpi-lesson.mjs` ตอนนี้ใช้สูตรนี้อยู่ ถ้าจะใช้ต่อต้องแก้
- ปีงบประมาณไทยเริ่ม **1 ตุลาคม** → ตุลาคม 2568 = **ไตรมาส 1 ของปีงบ 2569**
- บันทึกประชุมมีทั้ง **มติ** และ **ข้อเสนอที่ยังไม่มีมติ** — งานสรุปที่ดีต้องแยกสองอย่างนี้
- ปี พ.ศ. ทั้งหมดในฝั่งไทย (ตอนนี้เด็คเขียน "20 พ.ย. 2026" — ปนสองระบบ)

**กติกาข้อมูล**

- ทุกไฟล์หรือกล่องข้อมูลตัวอย่างต้องมีบรรทัด `ข้อมูลสังเคราะห์เพื่อการฝึกอบรม — ไม่ใช่ข้อมูลผู้ป่วยจริง`
- ไม่มีชื่อผู้ป่วยจริง ไม่มี HN/AN ที่ดูเหมือนของจริง
- **อย่าคัดลอกชุดข้อมูล `workspace/data/` ของคอร์สมาใส่ repo นี้** — repo นี้เป็น public และชุดข้อมูลนั้นมีจุดที่ใช้สอนในห้อง (ตำแหน่งคำสั่งแฝง รายการซ้ำ ฯลฯ)
  ถ้าเผยแพร่ ผู้เรียนจะเห็นเฉลยก่อนเข้าห้อง — แต่งตัวอย่างใหม่ในแนวเดียวกัน หรือถามผมถ้าต้องการตัวอย่างเฉพาะ

**งานออกแบบที่ขอให้เสนอมาก่อนลงมือ** — ส่งเป็นข้อความหรือ PR ร่างก็ได้

1. โหมด **"ลองจัดห้อง"** จะกลายเป็นอะไรในโรงพยาบาล (เช่น จับคู่งานกับเครื่องมือที่เหมาะ / จัดของขึ้นโต๊ะ)
2. ตัวละครแต่ละตัวเป็นใคร ขอให้ช่วยงานอะไร
3. ป้ายแบรนด์ (`KUBO’S COSMIC GUESTHOUSE`) จะเปลี่ยนเป็นอะไร — ถ้าเป็นไทย ต้องเอา `letter-spacing` ออกจาก `.brand`

---

## ส่วนที่ 3 — รายบท

**ใช้ชื่อบทตามตารางนี้** — เป็นชื่อเดียวกับที่พิมพ์อยู่ใน cheatsheet ที่แจกผู้เรียนไปแล้ว ถ้าเปลี่ยนชื่อ เอกสารเก่าจะชี้ผิด

| รหัส | ชื่อบท (ใช้ชื่อนี้) | ตอนนี้ | ต้องทำ |
|---|---|---|---|
| QI-01 | โต๊ะทำงานของ agent | ข้อมูลในบริบท | **คงแนวทางเดิม** — ภารกิจ 3 ขั้นกับหมายเหตุ "4 การ์ดไม่ใช่ benchmark ของ context rot" ซื่อตรงกว่าของเดิมในคอร์ส · **ห้ามเพิ่มมาตรวัด "% ความแม่นยำ"** (ของเดิมในคอร์สเป็นสูตรที่แต่งขึ้น เราจะแก้สไลด์ฝั่งเราแทน) |
| QI-02 | เลือกแนวทางให้ถูก | จัดบริบท | ผูกแต่ละแนวทางกับเครื่องมือตามส่วนที่ 1 · เพิ่มโจทย์ **"ปัญหานี้ใช้แนวทางไหน"** 4–6 ข้อ แต่ละข้อ**อธิบายทุกตัวเลือกที่ผิด** ว่าทำไมไม่ใช่ (สไลด์สัญญาไว้ว่าบทนี้ "มีโจทย์เพิ่มพร้อมคำอธิบายว่าทำไมไม่ใช่แนวทางอื่น") |
| QI-03 | ประกอบคำสั่ง | คำสั่งชัดเจน | ตอนนี้ 3 ส่วน (งาน · ข้อมูล · รูปแบบ) → **ให้ครบ 4 ส่วนตาม Lab 1** ดูตารางด้านล่าง · ตัวเลือก "ซ่อนรูปแบบผลลัพธ์" ต้องสื่อว่า *ไม่ตรงแบบฟอร์มที่ต้องส่ง → ถูกตีกลับ* ไม่ใช่ "ใช้ค่าเริ่มต้นก็ได้" |
| QI-04 | วงจรการทำงานของ agent | วงจร Agent | วงจร 5 ขั้นตอนนี้ตรงกับสไลด์แล้ว (คิด → เลือก → ลงมือ → อ่านผล → ตัดสินใจ) ดีกว่าของเดิม · **เพิ่ม "ระดับความอิสระ 4 ระดับ"** (ตารางด้านล่าง) และหนึ่งบรรทัดต่อขั้นว่า *อะไรเข้าโต๊ะเพิ่มในขั้นนี้* |
| QI-05 | เอกสารที่สั่ง agent ได้ | ข้อมูลภายนอก | เทียบเท่าของเดิมแล้ว · เพิ่มได้: ตัวอย่างรายงานอุบัติการณ์ที่มีข้อความแฝง เทียบสรุปแบบโดนสั่งกับแบบปลอดภัย และการป้องกันด้วยการเขียนข้อห้ามไว้ใน `CLAUDE.md` |
| QI-06 | ภาษาไทยกิน token เท่าไร | โทเคนไทย–อังกฤษ | ใช้ตัวเลขที่วัดกับ Claude (ส่วนที่ 4) · ตัวนับ o200k ที่มีอยู่ต้องติดป้ายว่าเป็น tokenizer ของ OpenAI |
| QI-07 | ห้องทำงานของคุณ | ห้องทำงาน | ชื่อ 8 ชิ้นตามส่วนที่ 1 · ทุกชิ้นบอกไฟล์จริงและ Lab · ปุ่มลัด = Compress · เพิ่มสองจังหวะที่สไลด์อ้างถึง (ด้านล่าง) |

**QI-03 — คำสั่ง 4 ส่วน (จาก Lab 1)**

| ส่วน | ตอบคำถามว่า | ถ้าไม่ใส่จะเกิดอะไร |
|---|---|---|
| 1. บริบท | ฉันเป็นใคร ที่ไหน ข้อมูลอยู่ไฟล์ไหน มีกติกาอะไรที่ agent ไม่มีทางรู้เอง | agent เดาจากความรู้ทั่วไป ซึ่งไม่ใช่กติกาโรงพยาบาลคุณ |
| 2. งาน | ให้ทำอะไร กับข้อมูลช่วงไหน คำนวณด้วยสูตรอะไร | agent เลือกวิธีนับเอง แล้วเลขไม่ตรงนิยาม |
| 3. รูปแบบผลลัพธ์ | หน้าตายังไง หัวข้ออะไร ยาวแค่ไหน เอาไปวางที่ไหน | agent จัดหัวข้อเอง ไม่ตรงฟอร์มที่ทีมนำกำหนด |
| 4. ข้อห้าม | อะไรที่ห้ามทำเด็ดขาด และถ้าข้อมูลไม่พอให้ทำอย่างไร | agent เติมช่องว่างด้วยการเดา แล้วเราจับไม่ได้ |

**QI-04 — ระดับความอิสระ 4 ระดับ (จากสไลด์)**

| ระดับ | มันทำอะไร | เหมาะกับงานแบบไหน |
|---|---|---|
| 1 · แนะนำ | บอกว่าควรทำอะไร ไม่ลงมือ | ตัดสินใจที่มีผลกระทบสูง |
| 2 · ถามก่อนทำ | ขออนุมัติทุกขั้น | เขียนทับไฟล์ · ส่งออกนอกหน่วยงาน |
| 3 · ทำแล้วรายงาน | ลงมือเลย แล้วบอกว่าทำอะไรไป | รวบรวมไฟล์ · จัดรูปแบบ · ร่างฉบับแรก |
| 4 · ทำเองทั้งหมด | ไม่ต้องรายงาน | งานที่ย้อนกลับได้และตรวจง่าย |

ข้อสรุปของสไลด์: *ไม่มีระดับไหนถูกที่สุด คำถามคืองานชิ้นนี้ควรอยู่ระดับไหน*

**QI-07 — สองจังหวะที่สไลด์ส่งผู้เรียนมาดู**

1. **"ปิดหน้าต่างแล้วเปิดใหม่"** (กดที่โต๊ะทำงาน) — โต๊ะว่างเปล่า ทุกอย่างที่อธิบายไปหาย แต่**ตู้เซฟกับไฟล์ในโปรเจกต์ยังอยู่**
   และตู้เซฟ (`CLAUDE.md`) ถูกอ่านกลับขึ้นโต๊ะเองในรอบใหม่ — นี่คือเหตุผลของ Lab 2
2. **หน้าต่างขออนุมัติมี 4 ทาง ไม่ใช่ 2** — ผู้ช่วยขอเขียนทับไฟล์รายงานที่คนแก้ด้วยมือไปแล้ว
   - **รับ** → ไฟล์ถูกเขียนทับ ของที่แก้ด้วยมือหาย
   - **แก้ก่อนรับ** → เปลี่ยนเป็นบันทึกไฟล์ใหม่ ของเดิมอยู่ครบ (ใช้บ่อยที่สุดในงานจริง)
   - **ตอบกลับ** → คุยกลับแทนการตัดสิน ผู้ช่วยเสนอแผนใหม่ ยังไม่ลงมือ
   - **ไม่สนใจ** → งานค้างรอ ไม่เดินหน้าเอง
   - แล้วตามด้วยปุ่มสุดท้าย **"จำลอง: ไม่มีใครอยู่หน้าจอ"** → งานหยุดค้างเงียบ ๆ ไม่มีอะไรเตือน —
     *ยิ่งปล่อยให้ทำเองมาก ยิ่งต้องรู้ล่วงหน้าว่ามันจะไปหยุดรอที่ไหน*

ตอนนี้บทสิทธิ์อนุญาต ("คุณเลือกขอบเขตครั้งนี้") มี 2 ทาง · บท "โต๊ะบริบท: เริ่มกะใหม่" ใกล้จังหวะที่ 1 แล้ว
แต่เขียนว่า *"บางระบบอาจโหลดคำสั่งให้อยู่แล้ว"* — ใน Claude Code `CLAUDE.md` ถูกอ่านขึ้นโต๊ะเองทุก session ให้พูดตรง ๆ ว่าตู้เซฟรอด ส่วนที่คุยไว้หาย

---

## ส่วนที่ 4 — QI-06: ตัวเลขที่วัดกับ Claude แล้ว

`js-tiktoken o200k_base` ที่ใช้อยู่เป็น tokenizer ของ **OpenAI** — นับไทยได้**ราวครึ่งเดียว**ของ Claude
ผู้เรียนที่วางบันทึกประชุมจริงลงไปจะประเมินต่ำไปครึ่งหนึ่ง

วัด 21 ก.ย. 2569 · Claude Code 2.1.278 · Opus 5 และ Sonnet 5 ได้เลขเดียวกันทุกตัว

| ข้อความ | Claude ไทย | Claude อังกฤษ | o200k ไทย | o200k อังกฤษ |
|---|---|---|---|---|
| `tokenTexts.short` (ประโยคกุญแจ) | 41 | 32 | 22 | 19 |
| `tokenTexts.long` | 110 | 55 | 54 | 36 |
| บันทึกประชุมทีมนำคุณภาพ ~8 KB | 2,587 | 1,293 | 1,259 | 793 |

- เอกสารเนื้อหาเดียวกัน **ไทยใช้ token ราว 2 เท่าของอังกฤษ** กับ Claude (o200k ได้แค่ 1.6 เท่า)
- ต่อคำ: ไทย ≈ 3–4 token · อังกฤษ ≈ 2 token (ของเดิมในคอร์สเขียน 2–3 กับ 1 — แก้ฝั่งเราแล้ว)
- กติกาประมาณแบบง่ายที่ใกล้ของจริง: **อักษรไทย 1 ตัว ≈ 1 token + อักขระอื่น ÷ 3** — เอกสารยาวคลาดราว 5% ข้อความสั้นคลาดได้ถึง 20%

**ที่ขอให้ทำ**

- แสดงตัวเลข Claude ที่วัดแล้วสำหรับตัวอย่างที่ตายตัว · ส่วนช่องพิมพ์อิสระ ใช้ o200k ต่อได้ แต่ติดป้ายว่า *"tokenizer ของ OpenAI — ของ Claude นับไทยได้ราว 2 เท่าของตัวเลขนี้"* หรือเปลี่ยนเป็นกติกาประมาณด้านบน
- **ห้ามมี network request ตอนพิมพ์** — ผู้เรียนจะวางบันทึกประชุมจริงของหน่วยงานลงไป
- ถ้าเปลี่ยน `tokenTexts` เป็นประโยคโรงพยาบาล ตัวเลขในตารางจะใช้ไม่ได้ — ส่งประโยคคู่ใหม่มา ผมวัดให้ หรือวัดเองด้วยวิธีนี้:

```sh
# ในโฟลเดอร์ว่าง — ส่งคำสั่งเดียวกันสองแบบ มี/ไม่มีข้อความ แล้วลบ input tokens กัน
F='--system-prompt x --tools "" --strict-mcp-config --disable-slash-commands --setting-sources "" --output-format json --no-session-persistence'
eval claude -p '"Reply with exactly: OK"' $F < /dev/null | jq '.usage.input_tokens + .usage.cache_creation_input_tokens + .usage.cache_read_input_tokens'
eval claude -p '"Reply with exactly: OK

<ข้อความที่จะวัด>"' $F < /dev/null | jq '.usage.input_tokens + .usage.cache_creation_input_tokens + .usage.cache_read_input_tokens'
```

ต้องใส่ flag ตัด context ทุกตัว — ไม่งั้นฐานจะอยู่ที่หลายแสน token และแกว่งหลายหมื่นระหว่างรอบ (เจอมาแล้ว)

---

## ส่วนที่ 5 — เด็ค Agent Team (`app/`)

ตรวจซ้ำบน `main` ล่าสุด (`c924c7d`) แล้ว ทุกข้อยังอยู่

**D1 · ขึ้นเว็บได้** — `vercel.json` deploy แค่ `website/` เด็คจึงยังไม่ขึ้นที่ไหนเลย
`npm run build` export แบบ static ได้ (`dist/client/`) · ขอให้อยู่ที่ path คงที่ เช่น `/team/` · โมเดลที่เด็คเรียก (`/cube-guy.glb`, `/qi07-room.glb`) มีใน `website/` อยู่แล้ว

**D2 · เนื้อหา**

- **ค่าเริ่มต้นเป็นไทย** (ตอนนี้อังกฤษ) · แปลชื่อบทบาท Brief Analyst / Writer / Quality Checker / Orchestrator
- ฉากประกาศกิจกรรมเปลี่ยนเป็นงานโรงพยาบาลได้ เช่น ประกาศอบรมการรายงานอุบัติการณ์ — **คงจุดวันที่ผิดที่ให้ checker จับไว้** และหลังเปลี่ยนต้องรันทดสอบใหม่ (ผมรันให้ได้)
- **เพิ่มหนึ่งสไลด์ที่บอกตรง ๆ ว่างานส่วนใหญ่ agent ตัวเดียวพอ** — Lab 5 ของคอร์สวัดแล้วว่า agent เดียวผ่านเกณฑ์ครบ
  ทีมคุ้มเมื่อต้องการ*ผู้ตรวจอิสระที่ไม่ได้เขียนร่างเอง* หรือเมื่อ*อ่านของดิบมากแต่ต้องการข้อสรุปสั้น*
  ตอนนี้ประโยคนี้อยู่แค่ใน `ORCHESTRATOR.md` ("a small task may not need the full team") ไม่อยู่บนสไลด์
- ชุด event-kit: ผมรันกับ Claude Code จริง 6 รอบ **ผ่านเกณฑ์ใน `TESTS.md` ครบ 6/6** — ไฟล์ agent ใช้รูปแบบถูกต้องแล้ว

**D3 · ตัวอักษรและโค้ด** (นับจากซอร์ส — ต้องวัดซ้ำจากหน้าจริงหลัง D1 เพราะบางข้ออาจเป็นป้ายละตินล้วนซึ่งถ่างได้)

- `app/orchestration.css` ใช้ `font-family: Arial, 'Noto Sans Thai'` — Noto ไม่ได้ bundle มา ส่วน IBM Plex Sans Thai Looped ที่มีอยู่ไม่ถูกใช้ในหน้าหลัก
- `font-size` ต่ำกว่า 12px **11 กฎ** (9–11px) เช่น `.or-eyebrow` `.or-kicker` `.or-handoff small` `.or-kubo-label span`
- `letter-spacing` **18 กฎ** · `text-transform: uppercase` **3 กฎ** (`.or-kicker` `.or-brief-fields span` `.or-delivery span`)
- `app/layout.tsx` hard-code `<html lang="en">`
- `app/interactive-runtime.js` ไม่มี reduced-motion เลย (Guesthouse ทำไว้ดีแล้ว ใช้แบบเดียวกันได้)
- ภาคผนวก `#workspace` ใช้ `<Office/>` โดยไม่ส่ง `presentationMode` — ถ้าโฮสต์นอก `*.vercel.app` / `*.github.io` (เช่นโดเมนของเราเอง)
  `app/legacy-presentation.tsx` จะเปิด `EventSource('/api/events')` → 404 แล้ว retry ไม่หยุด (บน vercel.app ไม่เป็น เพราะมี guard `publicDemo`)

**D4 · ล้าง** — `app/thai-lessons.mjs` ยังมีคำว่า **Codex** 4 บรรทัด · default export ของ `legacy-presentation.tsx` ไม่มีใครเรียก (เด็คใช้แค่ `Workspace`)

---

## ส่วนที่ 6 — กติกาตัวอักษรไทย และวิธีตรวจ

| กติกา | ค่า | ทำไม |
|---|---|---|
| ขนาดข้อความไทยบนเว็บ | **≥ 12px** ที่ render จริง | ต่ำกว่านี้สระและวรรณยุกต์อ่านไม่ออก |
| ระยะบรรทัดของไทยที่ตัดบรรทัด | **≥ 1.55** หรือ **≥ 43.4px** | ไทยซ้อน 4 ชั้น บรรทัดบนชนสระบรรทัดล่าง |
| `letter-spacing` บนไทย | **ห้าม** (ละตินล้วนได้ เช่น `QI-01`) | ถ่างแล้วสระหลุดจากพยัญชนะ |
| `text-transform: uppercase` | **ห้าม** | ไทยไม่มีตัวใหญ่ มักแปลว่าคัดลอก CSS มา |
| ตัวเอียงบนไทย | **ห้าม** | synthetic italic ทำให้หัวอักษรตัน |
| ฟอนต์ | ต้องมีอักษรไทยจริง **และประกาศทุกน้ำหนักที่ใช้** | ไม่ประกาศ = เบราว์เซอร์ทำตัวหนาปลอม (เจอใน Guesthouse — แก้ใน #5) |

**ตรวจจากหน้าจริง ไม่ใช่จากซอร์ส** — CSS บอกไม่ได้ว่าฟอนต์ไหนถูกวาดจริง
ที่ผมใช้ใน #5: เปิด headless Chromium เดินทุกบท เปิด `<details>` ทุกอัน ที่ 1280 · 375 · 320 px
อ่าน `getComputedStyle` ของทุก element ที่มีอักษรไทย และถาม CDP `CSS.getPlatformFontsForNode` ว่าถูกวาดด้วย face ไหน
(ตัวหนาปลอมจะขึ้นชื่อ face Regular ทั้งที่ `font-weight: 700`) · นับบรรทัดที่ตัดจริงด้วย `Range.getClientRects()`
ต้องการสคริปต์นี้บอกได้ หรือส่ง branch มา ผมรันให้

**เช็กลิสต์จาก ui-gates ที่ใช้กับทุก PR หน้าเว็บ**

- ไม่มี scroll แนวนอนที่ 320–1920 px
- ป้ายปุ่ม ลิงก์ แท็บ ไม่ตัดเป็นสองบรรทัด (ย่อคำก่อน → `nowrap` → ซ่อนที่จอแคบ ตามลำดับ) และ `nowrap` ต้องไม่ดันของอื่นหลุดจอ
- ทุกแอนิเมชันมีทางเลือก `prefers-reduced-motion`
- ไม่มีตัวเลขที่ไม่ได้วัดจริง

---

## ส่วนที่ 7 — วิธีส่งงาน

- **หนึ่ง PR หนึ่งเรื่อง** ตามลำดับ: ศัพท์ QI-07 → QI-02 → QI-03 → QI-04 → QI-06 → เนื้อเรื่องโรงพยาบาล → เด็ค D1–D4
  (ศัพท์ก่อนเนื้อเรื่อง เพราะถ้าเขียนเรื่องใหม่ด้วยศัพท์ผิด ต้องเขียนซ้ำ)
- ก่อนเปิด PR: `npm run check:course` (หลัง #4 merge) · `node --test` ทุก `*.test.mjs` · `npm run build:website` · เปิดดูทั้งไทยและอังกฤษที่จอแคบและจอกว้าง
- คำอธิบาย PR บอกว่า**วัดอะไร ได้เท่าไร** ไม่ใช่แค่ "แก้แล้ว" — ถ้ายังไม่ได้ตรวจข้อไหน เขียนว่ายังไม่ได้ตรวจ
- ข้อความภาษาไทยทุกบรรทัด ผมจะอ่านบน preview ก่อน approve

ขอบคุณมากครับ — งาน 3D กับตัวละครดีมาก สิ่งที่ขอทั้งหมดในนี้คือให้มันพูดภาษาเดียวกับห้องเรียน
